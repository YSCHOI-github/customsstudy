---
name: 무역실무-factcheck
description: 무역실무 단권화 MD 파일을 NotebookLM 무역실무챗봇(대외무역법·외국환거래법·관세법·UCP 600·Incoterms 2020·CISG·운송협약·ICC·영국 MIA 1906·뉴욕협약·무역보험법 원문 보유)과 대조하여 팩트체크하고 오류·보완사항을 자동 수정한 뒤 _factcheck_report.md에 기록한다. /무역실무-factcheck <파일경로> 형식으로 호출.
---

# 무역실무 단권화 팩트체크

무역실무 단권화 MD 파일을 NotebookLM 무역실무챗봇(대외무역법·외국환거래법·관세법·UCP 600·Incoterms 2020·CISG·헤이그-비스비 규칙·함부르크 규칙·로테르담 규칙·영국 MIA 1906·협회화물약관(ICC A/B/C)·뉴욕협약·무역보험법 원문 보유)과 대조하여 팩트체크하고, 오류와 보완사항을 자동 수정한 뒤 결과를 `_factcheck_report.md`에 기록한다.

## 사용법

```
/무역실무-factcheck <md_파일_경로>
```

예시:
```
/무역실무-factcheck 02_무역실무/10_단권화/03_02_신용장UCP600.md
```

---

## 실행 지시문

ARGUMENTS에 지정된 MD 파일 경로를 대상으로 아래 STEP을 순서대로 실행하라.
사용자 확인을 기다리지 말고 STEP 1~5를 끝까지 자동 실행하라.

---

### STEP 1 — 파일 읽기 및 청크 분할

1. `Read` 도구로 대상 MD 파일 전체를 읽는다.
2. `##`로 시작하는 섹션 헤더를 기준으로 청크를 나눈다.
3. 청크 크기를 조정한다:
   - 한 청크가 150줄을 초과하면 섹션을 나눠 두 청크로 분리한다.
   - 한 청크가 40줄 미만이면 인접 섹션과 합쳐 하나의 청크로 만든다.
4. 최종 청크 수는 파일 크기에 따라 조정한다:
   - 파일이 300줄 미만이면 3~4개, 300줄 이상이면 5~6개를 목표로 한다.

> ⚠️ 무역실무 파일은 최대 677줄에 달하므로 청크 수 상한(6개)을 유연하게 적용한다.

---

### STEP 2 — 브라우저 에이전트 호출 및 팩트체크 질의

`browser_subagent` 도구를 호출하여 무역실무챗봇에서 각 청크별 팩트체크를 수행한다.

- **URL 접속**: `https://notebooklm.google.com/notebook/93149bbc-9114-47e4-968d-8ec5c459e133` 로 이동
- **Task 설정**: 분할된 모든 청크에 대해 순차적으로 채팅창에 질의하고 답변을 수집하도록 지시한다.

**질의 프롬프트 양식:**
```text
다음은 무역실무 단권화 학습자료입니다. 이 노트북에 있는 원문 자료(대외무역법·외국환거래법·관세법 등 한국 법령, UCP 600·Incoterms 2020·CISG·헤이그-비스비 규칙·함부르크 규칙·로테르담 규칙·영국 MIA 1906·협회화물약관(ICC A/B/C)·뉴욕협약·무역보험법 등 국제규범)를 기준으로, 아래 내용 중 원문과 다르거나 잘못된 내용을 구체적으로 지적해 주세요. 해당 내용과 관련된 조문·조항·규칙 번호를 함께 명시해 주세요. 틀린 내용이 없으면 '이상 없음'이라고 답해주세요.

[여기에 청크 텍스트 삽입]
```
모든 청크의 답변 결과를 메인 에이전트에게 반환하도록 지시한다.

---

### STEP 3 — 결과 취합 및 분류

브라우저 에이전트로부터 반환된 모든 청크의 응답을 합쳐 아래 두 범주로 분류한다:

- **🔴 오류**: 원문과 명확히 다른 내용 (조문번호 오류, 존재하지 않는 요건, 잘못된 당사자 등)
- **🟡 보완**: 표현이 불충분하거나 누락된 내용 (법령 용어 보완, 조건 추가, 조항 번호 누락 등)

이상 없는 섹션도 목록으로 기록해 둔다.

---

### STEP 4 — 자동 수정 적용

`Edit` 도구를 사용해 대상 MD 파일에 수정을 적용한다.

규칙:
- 🔴와 🟡 모두 수정한다.
- 기존 포맷(헤더 레벨, 표, 코드블록, 들여쓰기)을 유지한다.
- 조문번호·요건·표현 등 내용만 최소한으로 변경한다.
- 섹션 추가·삭제·재구성은 하지 않는다.

---

### STEP 5 — 결과를 _factcheck_report.md에 기록

대상 MD 파일과 **같은 폴더**에 `_factcheck_report.md`가 있으면 파일 끝에 append하고, 없으면 새로 생성한다.

아래 형식으로 기록한다:

```markdown
## <YYYY-MM-DD> | <파일명>

### 🔴 수정됨 (<N>건)
- [항목 설명]: `[변경 전]` → `[변경 후]` (근거: [조문·조항·규칙 번호])

### 🟡 수정됨 (<N>건)
- [항목 설명]: [변경 내용] (근거: [조문·조항·규칙 번호])

### ✅ 이상 없음
- [이상 없는 섹션 목록]

---
```

수정 사항이 없는 범주는 해당 헤더를 생략한다.
모든 작업이 끝나면 `_factcheck_report.md` 기록 완료 여부를 한 줄로 사용자에게 알린다.
